# Nib domain context

Nib is a static-by-default React site framework. File pages, Markdown pages,
and typed data pages become page routes. Only explicit islands ship browser
React.

## Routing terms

- **Page route** — a route rendered through React, layouts, the site shell, and
  the island collector.
- **Resource route** — a virtual static route whose adapter supplies a body and
  content type, such as `rss.xml` or `sitemap.xml`.
- **Redirect route** — a route that points to an internal path or an absolute
  HTTP(S) URL. Static builds emit redirect HTML; development responds with an
  HTTP redirect.
- **Resolved route** — the immutable public description of a page, resource, or
  redirect route after file routes, configured redirects, and plugin
  registrations have been merged and checked for collisions.
- **Route provider** — a plugin adapter that inspects the initial project routes
  and contributes page, resource, or redirect routes before the resolved-route
  inspection phase.
- **Trailing-slash policy** — `ignore`, `always`, or `never`; it controls
  canonical public paths, development/preview redirects, and static page
  artifacts. `never` publishes extensionless leaf files and directory indexes
  for route parents; the other policies publish directory-index HTML.
- **Route publication** — the framework-owned mapping from a resolved public
  route to its canonical href, static artifact, preview request, and redirect.
- **Document head contribution** — typed site, page, or renderer-plugin elements
  merged into Nib's generated document head with escaped output.
- **Publication manifest** — `.nib/publication.json` in the deployed client
  directory, mapping canonical routes to artifacts and response metadata.
- **Page-source collection** — the validated entries generated by a page source,
  reused as a typed collection rather than parsed a second time.

## Extension rules

- App configuration owns project-specific Vite adapters.
- Plugins may contribute page-source adapters and route providers before route
  resolution.
- Plugins may inspect resolved routes but cannot mutate them.
- Renderer plugins may alter ordinary static page output but cannot alter island
  markup or hydration metadata.
- Site and page metadata, plus renderer plugins' `head` hook, may contribute
  typed document-head elements. Nib owns ordering, escaping, and validation.
- Nib owns route collisions, path normalization, output filenames, base paths,
  redirect safety, and the publication manifest.
- Development and preview host exposure is explicit: loopback remains the
  default and callers add trusted names through `allowedHosts` or the CLI.
- Nib owns the Vite import boundary for page-source renderers that depend on a
  plugin transform; configuration only declares their module and export.
